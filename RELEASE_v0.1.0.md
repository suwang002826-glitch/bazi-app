# 八字排盘引擎 v0.1.0 正式版交付说明
**交付日期**：2026-07-04
**版本状态**：稳定可用，已同步GitHub
**验收基准**：「问真八字」固定口径，128条测试用例100%通过

---

## 一、版本核心信息
| 项 | 内容 |
|----|------|
| 版本号 | v0.1.0 |
| 规则基线 | 已锁死，不随输入变更：<br>• 立春精确时间换岁<br>• 十二节精确时间换月<br>• 23点整换日柱<br>• 不区分早晚子<br>• 默认关闭真太阳时/夏令时<br>• 默认经度120°E（北京时间）、纬度39°N |
| 核心完成能力 | 四柱核心排盘 + 1901-2100农历转换 + 问真基线全量验证 |
| 测试结果 | 单元测试90/90 PASS<br>主验证38/38 PASS<br>农历数据包73028条1901-2100全覆盖<br>农历验收全通过（含闰月、隐式农历识别） |

---

## 二、已验收模块清单
### 1. 核心排盘模块（已验收）
✅ 年柱计算：立春换岁边界精确到秒，跨年年柱正确
✅ 月柱计算：节气换月边界精确到秒，五虎遁月干正确
✅ 日柱计算：23点整换日逻辑正确，早子时/夜子时干支一致
✅ 时柱计算：十二时辰地支匹配正确，五鼠遁时干正确
✅ 夏令时处理：1986-1991年夏令时区间正确，开始/结束边界处理正确
✅ 真太阳时处理：经度修正（每度4分钟）+均时差计算正确，无效经度自动容错

### 2. 农历转换模块（已验收）
✅ 1901-2100年全范围农历转公历：73028条记录，覆盖所有平月/闰月日期
✅ 隐式农历识别：传入lunarYear/lunarMonth/lunarDay时自动走农历转换，无需显式声明
✅ 闰月处理：所有闰月日期映射正确，无错位
✅ 越界报错：1901年前/2100年后农历日期正确返回错误提示

### 3. 附属字段模块（已验收）
✅ 十神计算：全十神生克阴阳规则正确，覆盖所有天干组合
✅ 地支藏干：十二地支本气/余气顺序符合子平法，无遗漏
✅ 六十甲子纳音：干支纳音匹配正确
✅ 空亡（旬空）：旬空计算正确，各柱空亡输出一致

### 4. 适配输出模块（已验收）
✅ 前端适配格式输出：包含四柱详情、十神、藏干、纳音、空亡、五行统计、节气信息、农历转换结果
✅ 输入格式化：支持公历/农历多种输入格式，自动归一化处理
✅ 错误容错：无效经纬度、非法日期、越界日期自动返回明确错误

---

## 三、版本边界说明（重要）
**当前版本不是完整命理分析系统**，仅完成基础排盘能力：
1. ✅ 已完成：四柱排盘、农历转换、基础附属字段计算
2. ❌ 暂未实现：大运/流年/流月排盘
3. ❌ 暂未实现：神煞、格局、旺衰判断等命理分析
4. ❌ 暂未实现：AI解读、合婚、择吉等衍生功能
5. ❌ 前端暂未接入完整农历选择器（后端能力已就绪，待前端对接）

---

## 四、后续迭代优先级（按顺序执行）
### P0 立即执行
1. **前端对接农历能力**：小程序/App端农历年月日选择器调用现有农历data-pack，移除“当前农历日期暂未覆盖”提示，支持农历生日输入排盘

### P1 下版本v0.2.0
2. **大运排盘功能**：实现顺逆排规则（阳男阴女顺排、阴男阳女逆排）、起运岁数计算、起运精确时间、大运干支列表输出，完成从“能排四柱”到“专业排盘工具”的关键升级

### P2 后续规划
3. 流年/流月排盘
4. 基础神煞计算
5. 格局与旺衰判断
6. AI命理解读

---

## 五、快速验证方式
拉取代码后执行以下命令验证版本完整性：
```bash
# 安装依赖（首次运行）
npm install

# 全量测试（单元+主验证）
npm test

# 单独跑单元测试
npm run test:unit

# 单独跑主验证
npm run test:verify
```
所有命令执行无报错即为版本完整可用。

---

## 六、文件清单
| 文件路径 | 说明 |
|---------|------|
| `code/utils/bazi/coreEngine.js` | 核心排盘引擎 |
| `code/utils/bazi/lunarDataPack.js` | 农历数据包加载与转换逻辑 |
| `code/data-packs/lunar/lunar-conversions-1901-2100.js` | 1901-2100农历转换数据包（73028条） |
| `code/utils/bazi/constants.js` | 规则常量配置（已锁死问真口径） |
| `code/utils/bazi/pageAdapter.js` | 前端输出适配层 |
| `scripts/verify.js` | 主验证脚本（38条用例） |
| `scripts/unit-test.js` | 单元测试脚本（90条用例） |
| `scripts/validate-bazi-acceptance.js` | 农历验收脚本 |
| `CHANGELOG.md` | 版本变更记录 |
| `RELEASE_v0.1.0.md` | 本交付说明 |
