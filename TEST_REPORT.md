# 测试报告（v0.4.2）

## 一、验收目标

本轮验证目标是确认 v0.4.2 在加入结果页交互、历史命例、城市选择、字段说明和问真神煞样本入口后，核心排盘链路不回退：

- 四柱、大运、流年、流月、农历输入、闰月输入继续通过既有验收。
- 结果页「大运流年」可使用后端输出的真实大运、流年、流月数据。
- 命例历史、本地保存和恢复不影响排盘结果。
- 字段说明只做标准信息解释，不加入主观断语。
- 神煞模块只建立样本采集和结构校验入口，不实现未验收算法。

## 二、执行结果

### 1. 完整命令回归

- 脚本：`npm test`
- 结果：222 / 222 PASS，0 FAIL

明细：

- 单元测试：139 / 139 PASS
- 问真基线验证：42 / 42 PASS
- 高风险边界验证：30 / 30 PASS
- 流年流月专项：11 / 11 PASS

### 2. 大运专项

- 脚本：`node scripts/test-dayun.js`
- 结果：PASS

### 3. 农历验收

- 脚本：`node scripts/validate-bazi-acceptance.js`
- 结果：PASS
- 覆盖：普通公历、立春前后、子时、农历真实输入、农历闰月真实输入、data-pack 外日期报错、隐式农历识别。

### 4. 农历数据包

- 脚本：`node scripts/validate-lunar-data-pack.js`
- 结果：PASS
- 覆盖：73028 条记录，1901-2100 年农历转换。

## 三、本轮新增验证点

- 城市快捷选择器可导出、可列出城市、可按地区匹配经纬度。
- 结果页默认折叠区块可导出，切换逻辑不污染原状态。
- 当前流年 helper 可按年份定位并格式化展示。
- 字段解释模块可导出并覆盖十神、藏干、空亡、纳音、神煞、十神旺衰。
- 问真神煞样本采集入口可生成模板、校验样本结构，并明确 pending 状态。

## 四、风险与限制

1. 神煞规则仍未实现，当前只完成问真样本采集和校验入口。
2. 每个神煞至少需要问真八字命中样本和明确计算规则，建议同时补充不命中样本。
3. AI 断语仍未开发，后续必须基于已验收的结构化字段生成。
4. 小程序真实设备体验仍需前端在微信开发者工具和手机上复测。

## 五、结论

v0.4.2 当前核心回归通过。大运、流年、流月、农历 data-pack、历史命例、城市选择和字段解释可以进入前端联调；神煞仍保持样本采集阶段，不能进入正式算法实现。
