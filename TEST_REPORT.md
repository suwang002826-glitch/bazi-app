# 测试报告（v0.3.1）

## 一、验收目标

按你的要求完成“边界样本独立化 + 全链路回归无失败”验证，确保：
- 现有核心逻辑不改动前提下通过新增边界集合
- 全量回归（单元 + 集成 + 边界 + 流年）100% 通过

## 二、执行结果

### 1) 单元测试
- 脚本：`node scripts/unit-test.js`
- 结果：66 / 66 通过

### 2) 基线集成验证（问真基线）
- 脚本：`node scripts/verify.js`
- 结果：42 / 42 通过

### 3) 边界样本验证
- 脚本：`scripts/verify-edge.js`
- 用例文件：`scripts/edge-cases.js`
- 结果：30 / 30 通过

### 4) 流年专项
- 脚本：`node scripts/test-liunian.js`
- 结果：11 / 11 通过

### 5) 大运专项
- 脚本：`node scripts/test-dayun.js`
- 结果：通过

### 6) 完整命令回归
- 脚本：`npm test`（当前环境等价于逐条执行 unit + verify + verify-edge + test-liunian）
- 结果：0 失败

## 三、边界覆盖明细（30条）

- `solar-term-year`（立春界）：5 条
  - 涵盖立春前后、边界秒前/边界秒后
- `solar-term-month`（月柱界）：6 条
  - 覆盖小寒、惊蛰等关键节气边界
- `day-hour`（23:00 / 00:00 / 早晚子时）：11 条
  - 覆盖默认与早晚子时开关对比
- `summer-time`（夏令时）：4 条
  - 覆盖起始/结束边界及前后时点
- `true-solar`（真太阳时）：4 条
  - 覆盖乌鲁木齐（87.6°）、东经 126.6°，以及跨日边界场景

## 四、主要风险与说明

1. 边界样本全部采用固定快照（问真口径），若口径更新需同步修订 `edge-cases.js` 与版本说明  
2. 边界验证强依赖精确节气包，若节气数据包版本更新，需要再跑 full suite 做回放  
3. 夏令时与真太阳时仍为“高敏感”参数入口，需要保持 UI 开关与适配层映射稳定

## 五、提交状态（可对外同步）

- 本阶段验收通过：边界套件 + 既有基线套件 + 单元回归 + 流年专项全部过线
- v0.3.1 边界补丁文档已同步更新（`RELEASE_v0.3.1.md`）
- 进入下一级开发前的“可上线稳定态”条件满足

## 六、建议动作

- 将 `scripts/edge-cases.js` 纳入每次迭代的 PR 模板必测项
- 所有新规则改动前先补“边界期望快照”，再执行全量回归
